📌 5. TRABALHANDO COM REPOSITÓRIOS REMOTOS - GIT PULL, GIT REMOTE, GIT PUSH
===========================================================

📤 GIT PUSH - ENVIANDO COMMIT PARA O REPOSITÓRIO REMOTO
===========================================================

🤔 O QUE É?
===========================
• O comando git push envia **seus commits locais** para um repositório remoto (ex: GitHub, GitLab, Bitbucket).  
• Permite **compartilhar alterações** com outros colaboradores.  
• É geralmente a **última etapa** após adicionar (`git add`) e comitar (`git commit`) suas alterações.  
• Mantém o **histórico remoto atualizado** e sincronizado com o local.  

===========================================================

1️⃣ PUSH SIMPLES
===========================
# Envia a branch atual para o remoto 'origin'
git push origin main

📌 Explicação:
- `origin` → nome do repositório remoto (padrão)
- `main` → branch local que você quer enviar
- Se a branch não existir no remoto, o Git **cria automaticamente** uma nova branch
- Ideal para **atualizações frequentes e commits pequenos**  

===========================================================

2️⃣ PUSH DE TODAS AS BRANCHES
===========================
# Envia todas as branches locais para o remoto
git push --all origin

📌 Quando usar:
- Útil quando você criou **múltiplas branches locais** que ainda não existem no remoto
- Evita enviar uma branch de cada vez  

===========================================================

3️⃣ PUSH DE TAGS
===========================
# Envia todas as tags locais para o remoto
git push --tags

📌 Explicação:
- Tags são usadas para marcar **versões importantes** (ex: v1.0, v2.0)
- Com `--tags` você garante que o remoto também terá **as tags de release**

===========================================================

4️⃣ DEFININDO UPSTREAM AO PRIMEIRO PUSH
===========================
# Útil quando a branch local não tem relação com o remoto
git push -u origin minha-branch

📌 Explicação:
- O `-u` (ou `--set-upstream`) define a branch remota **como padrão** para push e pull
- Depois disso, você pode usar simplesmente:
  git push  
- Isso simplifica comandos futuros e evita digitar `origin <branch>` toda vez  

===========================================================

5️⃣ FORÇANDO O PUSH
===========================
# Forçar envio de commits (sobrescreve histórico remoto)
git push --force

⚠️ ATENÇÃO:
- Pode sobrescrever commits de outros colaboradores 💥
- Use **apenas se souber exatamente o que está fazendo**
- Útil quando você **reescreveu histórico local** (ex: git rebase)  

===========================================================

💡 DICAS EXTRAS
===========================
• Sempre faça **git pull** antes de push para evitar conflitos; ❌  
• Use **git push -u** na primeira vez que criar uma branch; 🪾  
• Evite usar **--force** em branches compartilhadas sem coordenação ⚠️  
• Combine push com **boas mensagens de commit** para manter histórico limpo; 🧹  
• Prefira **commits pequenos e frequentes** ao invés de grandes mudanças de uma vez; e
• Se estiver colaborando, considere usar **pull requests** para revisar antes de push.